# sheratan-router-openai — Quality Patch
Branch: `chore/quality-mypy-ruff`

## Apply
```bash
git checkout -b chore/quality-mypy-ruff
git add -A
git commit -m "chore(router-openai): ruff + mypy + CI"
git push -u origin chore/quality-mypy-ruff
gh pr create --title "chore(router-openai): ruff + mypy + CI" --body "Adds ruff/mypy configs and quality workflow"
```
